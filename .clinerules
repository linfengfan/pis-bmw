# AI 执行家法 (.clinerules)

> 本文件定义 Claude 在本项目中的执行约束
> 这些规则优先级高于默认行为

---

## 一、代码质量规则

### 1.1 必须遵守
- 所有新增代码必须通过 TypeScript 类型检查 (`tsc --noEmit`)
- 公共 API 必须有 JSDoc 注释
- 错误处理：所有 async 操作必须 try/catch 或返回 Result 类型

### 1.2 禁止事项
- ❌ 禁止直接 `console.log`，使用项目日志工具
- ❌ 禁止 `any` 类型（新代码），旧代码需标注 `// TODO: type`
- ❌ 禁止提交 `.env` 或包含真实密钥的文件
- ❌ 禁止跳过测试直接合并

---

## 二、Git 操作规则

### 2.1 提交规范
- 提交信息格式：`<type>(<scope>): <subject>`
- type: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`
- scope: 受影响的模块（如 `pdf`, `server`, `cli`）
- 示例: `feat(pdf): 支持表格提取`

### 2.2 分支管理
- `main` — 稳定版本，仅通过 PR 合并
- `feat/*` — 新功能开发分支
- `fix/*` — Bug 修复分支
- 禁止直接在 `main` 上开发

---

## 三、文件操作规则

### 3.1 敏感文件
- 以下文件禁止提交到 git：
  ```
  .env, *.key, credentials.json, node_modules/, data/
  ```

### 3.2 大文件处理
- 单个文件 > 5MB 必须先确认
- PDF 测试文件存放在 `test.pdf`，不超过 10MB

---

## 四、任务执行规则

### 4.1 任务拆解
- 复杂任务必须先创建 `.catpaw/tasks.md` 子任务列表
- 每个子任务完成后标记 `[x]`
- 遇到阻塞必须立即报告，不继续盲目尝试

### 4.2 变更确认
- 任何破坏性变更（如删除文件、清空数据）必须先列出影响
- 删除旧代码前必须确认是否有替代实现
- 重构前必须确保有测试覆盖或手动验证计划

---

## 五、测试规则

### 5.1 测试覆盖率
- 新功能必须附带测试（单元测试或集成测试）
- 测试文件命名：`*.test.ts` 或 `*.spec.ts`

### 5.2 E2E 测试
- 使用 Playwright 进行端到端测试
- 测试脚本放在 `tests/e2e/` 目录
- 运行命令：`pnpm test:e2e`

---

## 六、调试与日志

### 6.1 日志级别
```typescript
DEBUG: 开发调试（开发环境）
INFO: 重要操作（如文件上传成功）
WARN: 可恢复的错误（如降级处理）
ERROR: 必须处理的错误（如解析失败）
```

### 6.2 调试模式
- 启用调试：`DEBUG=* pnpm dev`
- 查看特定模块：`DEBUG=pis-bmw:pdf pnpm dev`

---

## 八、原子化变更原则

### 8.1 单一职责
- 一个 PR / commit / 对话只做一件事
- ❌ 禁止："重构 + 修复 BUG + 添加测试"一起做
- ✅ 正确：每件事分开处理

### 8.2 幂等性
- 所有自动化操作必须可重入、不产生副作用
- 同一命令执行两次 = 同一结果
- ❌ 禁止：创建不可重复资源（不指定唯一 ID）

### 8.3 小步快跑
- 单次变更 ≤ 300 行核心逻辑
- 超过则拆分
- 优先合并"能跑的骨架"，再迭代细节

---

## 九、规划前置原则

### 9.1 触发条件
涉及 ≥2 个文件或跨模块的变更，必须生成三层文档：
1. `proposals/<feature>/proposal.md` — 需求背景与目标
2. `proposals/<feature>/design.md` — 技术方案
3. `.catpaw/tasks.md` — 实施清单

### 9.2 三层文档流程
```
1. proposal.md  → 人类审批 → 冻结需求
2. design.md    → 人类审批 → 确定方案
3. tasks.md     → AI 执行
4. Code Review  → 人类审批
5. 上线         → 人类放行
```

---

## 十、人类角色定义

### 10.1 指挥官三角
| 角色 | 操作 | 产出 |
|------|------|------|
| 决策 | 审批 proposal.md | 需求冻结 |
| 审批 | Code Review | 问题清单 |
| 把关 | 最终放行 | 上线许可 |

### 10.2 AI 禁止自行决定的操作
- 删除或修改 `openspec/specs/` 规范文件
- 修改 `.clinerules` 执行家法
- 删除或重命名核心模块
- 修改 API 端点行为
- 引入新的外部依赖
- 任何破坏性数据操作

---

## 十一、规范即文档原则

### 11.1 同步机制
| 触发事件 | 动作 |
|----------|------|
| 新功能上线 | 将 design.md 摘要合并至对应 spec |
| 重大重构 | 更新 spec 架构图 + 决策记录 |
| BUG 修复 | 在 spec 中补充"已知约束"章节 |

### 11.2 核心原则
- 代码即事实：spec 落后于代码时，以代码为准
- 禁止绕过：核心业务逻辑必须先有 spec
- Bridge Rule：跨系统配置通过 Bridge 文件引用，不复制内容

---

## 十二、降级策略

### 12.1 三级降级链
```
L1: 优先主流程
L2: 本地缓存或 fallback 配置
L3: 静默跳过，不抛错，不阻塞
```

### 12.2 应用场景
- AI 能力不稳定时
- 外部 API 调用时
- 非核心 UI 降级

---

## 十三、例外情况

如遇以下情况，可临时绕过规则但需说明原因：
1. 紧急修复生产环境 Bug
2. 迁移第三方库导致的类型兼容问题
3. 快速原型验证（后续会重构）

绕过规则后必须在提交信息中说明：
```
chore(exception): <reason>

原因：<为什么要绕过>
后续：<计划何时修复>
```